import React from "react";

const Addstdbutton = () => {
  return (
    <button
      type="submit"
      className="w-full hover:scale-105 py-5 transition-all duration-300 rounded-lg text-xl font-coolvertica text-white hover:text-black hover:bg-white hocus:outline hocus:outline-1 hocus:outline-gray-400 bg-blue-400"
    >
      Add Student
    </button>
  );
};

export default Addstdbutton;
